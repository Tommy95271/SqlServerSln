CREATE TABLE [dbo].[film_apply_station] (
    [id]        INT          NOT NULL,
    [stationNo] VARCHAR (10) NOT NULL,
    [name]      VARCHAR (30) NOT NULL,
    [dispOrder] INT          NOT NULL
);

